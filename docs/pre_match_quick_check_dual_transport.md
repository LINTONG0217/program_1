# 比赛前 1 分钟检查表

适用：

- 主车：`APP/test_uwb_two_anchor_localization.py`
- 从车：`APP/dual_slave.py`

目标：

- 上场前快速确认硬件、定位、通信、识别和协同搬运链路都处于可用状态

---

## 1. 供电与接线

- 主车、从车电池电量充足
- 雷达供电正常
- OpenArt mini 供电正常
- 无线通信模块供电正常
- DW3000 模块连接正常
- IMU、编码器线束没有松动

---

## 2. 主从程序

- 主车程序：`APP/test_uwb_two_anchor_localization.py`
- 从车程序：`APP/dual_slave.py`
- 主从烧录的配置没有弄反

---

## 3. UWB / IMU / 编码器

- 串口打印里能看到主车位姿持续更新
- 主车启动信息里有：`imu yaw fusion enabled: True`
- 原地轻转车体时 `yaw` 变化方向正确
- 推动车体时 `x/y` 有变化
- 两锚距离都在刷新，不是只有一个基站有值

---

## 4. 雷达

- 雷达有扫描输出，不是一直无数据
- 前方放一个物体时，主车能进入目标搜索/靠近流程
- 雷达前方角度窗口还是约 `-45` 到 `45` 度

---

## 5. OpenArt mini

- OpenArt mini 串口有输出
- 标签名和主控映射一致
- 比赛规则对应关系确认无误：
  - `标准网球 -> top`
  - `玩具沙袋 -> left`
  - `泰迪熊 -> right`

---

## 6. 双车无线通信

- 从车能收到主车状态
- 主车动时，从车会跟着动
- 主车停时，从车不会继续乱跑

---

## 7. 协同编队

- 普通跟随时，从车在主车侧边
- 协同推送时，两车间距会缩小
- 从车不会跑到主车前面

---

## 8. 赛场参数

- 场地尺寸按当前配置：
  - `FIELD_WIDTH_M = 3.2`
  - `FIELD_HEIGHT_M = 2.8`
- 基站位置与配置一致
- 起始位置和朝向与默认初始化假设一致

---

## 9. 最后 10 秒确认

- 主车能搜索目标
- 主车能识别目标
- 主车能进入 `transport_to_edge`
- 从车能同步进入协同搬运
- 清空后主车能停到 `match_done`

---

## 若现场只够检查 3 件事

优先检查：

1. `UWB + IMU + 编码器` 位姿是否正常
2. 主从无线通信是否正常
3. OpenArt 标签是否和搬运规则一致
